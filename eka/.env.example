# Path to the OAuth client credentials JSON you download from Google Cloud Console
GOOGLE_CREDENTIALS_PATH=credentials.json

# Where the cached OAuth token gets stored after your first login
GOOGLE_TOKEN_PATH=token.json

# Free API key from console.groq.com — used for answer generation
GROQ_API_KEY=your_groq_api_key_here
